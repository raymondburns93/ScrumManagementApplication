<Controls:MetroWindow x:Class="ScrumManagementApp.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ViewModels="clr-namespace:ScrumManagementApp.Client.ViewModels"
        xmlns:Views="clr-namespace:ScrumManagementApp.Client.Views"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        Title="Scrum Management Application" 
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize" 
        BorderBrush="{DynamicResource AccentColorBrush}"
        BorderThickness="1"
        WindowStartupLocation="CenterOwner">

    <!--<Window.Resources>
        <DataTemplate DataType="{x:Type ViewModels:LoginViewModel}">
            <Views:LoginView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type ViewModels:RegisterViewModel}">
            <Views:RegisterView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type ViewModels:HomeViewModel}">
            <Views:HomeView />
        </DataTemplate>
    </Window.Resources>-->

    <ContentControl Content="{Binding CurrentPageViewModel}" />
</Controls:MetroWindow>
